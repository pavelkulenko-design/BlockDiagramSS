<Window x:Class="BlockDiagramSS.BlockDiagramWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Block Diagram SS"
        Height="250" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Выберите чертежный вид:"/>
        <ComboBox x:Name="ViewSelector" Grid.Row="0" Margin="0,20,0,10"/>

        <TextBlock Text="Шаг (мм):" Grid.Row="1"/>
        <TextBox x:Name="StepBox" Grid.Row="1" Margin="100,0,0,0" Width="80"/>

        <TextBlock Text="Вертикальный отступ (мм):" Grid.Row="2"/>
        <TextBox x:Name="OffsetBox" Grid.Row="2" Margin="150,0,0,0" Width="80"/>

        <TextBlock Text="Длина линии (мм):" Grid.Row="3"/>
        <TextBox x:Name="LineLengthBox" Grid.Row="3" Margin="150,0,0,0" Width="80"/>

        <TextBlock Text="Фильтр цепей:" Grid.Row="4"/>
        <TextBox x:Name="CircuitFilterBox" Grid.Row="4" Margin="100,0,0,0" Width="120"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="OK" Width="80" Margin="0,0,10,0" Click="Ok_Click"/>
            <Button Content="Cancel" Width="80" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
